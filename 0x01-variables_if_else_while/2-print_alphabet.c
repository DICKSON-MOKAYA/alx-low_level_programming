#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
        {
        int lowercase = 'a';
	while (lowercase <= 'z');
	
        {
                putchar (lowercase);
		lowecase += 1;
        }
        putchar ('\n');
        return (0);
	}
